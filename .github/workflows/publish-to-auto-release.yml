name: "publish"
on:
  workflow_call:
    inputs:
      release-id:
        required: true
        type: string
    outputs:
      release-id:
        description: "The ID of the release created by the Tauri build."
        value: ${{ jobs.publish-tauri.outputs.release-id }}
      release-html-url:
        description: "The HTML URL of the release."
        value: ${{ jobs.publish-tauri.outputs.release-html-url }}
      release-upload-url:
        description: "The upload URL for the release."
        value: ${{ jobs.publish-tauri.outputs.release-upload-url }}
      artifact-paths:
        description: "The paths to the artifacts generated by the Tauri build."
        value: "${{ jobs.publish-tauri.outputs.artifact-paths }}"
      app-version:
        description: "The version of the application built by Tauri."
        value: ${{ jobs.publish-tauri.outputs.app-version }}

jobs:
  build-scripts:
    uses: ./.github/workflows/build-scripts.yml
    secrets: inherit
  get-version:
    needs: build-scripts
    uses: ./.github/workflows/get-version.yml
    secrets: inherit
  matrix-prep:
    needs: get-version
    uses: ./.github/workflows/matrix-prep.yml
    with:
      release-channel: ${{ needs.get-version.outputs.release-channel }}

  publish-tauri:
    needs: [build-scripts, get-version, matrix-prep]
    name: "${{ matrix.platform }} ${{ matrix.args }}"
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix: ${{fromJson(needs.matrix-prep.outputs.matrix)}}

    env:
      SCCACHE_GHA_ENABLED: ${{ matrix.platform != 'windows-11-arm' && 'true' ||  'false' }}
      RUSTC_WRAPPER: ${{ matrix.platform != 'windows-11-arm' && 'sccache' ||  null }}
      CARGO_TERM_COLOR: always
      CARGO_INCREMENTAL: 0
      release-channel: "stable"

    outputs:
      release-id: ${{ steps.build.outputs.releaseId }}
      release-html-url: ${{ steps.build.outputs.releaseHtmlUrl }}
      release-upload-url: ${{ steps.build.outputs.releaseUploadUrl }}
      artifact-paths: "${{ steps.build.outputs.artifactPaths }}"
      app-version: ${{ steps.build.outputs.appVersion }}

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: mozilla-actions/sccache-action@v0.0.9
        # disable on msvc due to a temporary limitation, see: https://github.com/Mozilla-Actions/sccache-action/issues/189
        if: matrix.platform != 'windows-11-arm'
        continue-on-error: true
      - uses: actions/checkout@v4

      - name: Cache pnpm modules
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-
      - uses: actions/setup-node@v4
        with:
          node-version: "lts/*"
      - uses: pnpm/action-setup@v4
        with:
          version: 10.x.x
      - run: pnpm i --frozen-lockfile

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || matrix.platform == 'windows-11-arm' && 'aarch64-pc-windows-msvc' || '' }}

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-latest' || matrix.platform == 'ubuntu-24.04-arm'
        uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: libwebkit2gtk-4.1-dev build-essential librsvg2-dev patchelf webkit2gtk-driver zstd xvfb curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev # libappindicator3-dev
          version: 1.0

      - run: pnpm install
      - uses: swatinem/rust-cache@v2
      - run: cargo fetch
        working-directory: src-tauri
      - name: Retrieve built scripts
        uses: actions/download-artifact@v4
        with:
          name: .dist
          path: /tmp/.dist/
      - name: Extract built scripts
        run: tar -xvf /tmp/.dist/.dist.tar
      - name: Set MSI Version
        if: matrix.platform == 'windows-latest' || matrix.platform == 'windows-11-arm'
        id: set-msi-version
        run: node .dist\set-msi-version.mjs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: tauri-apps/tauri-action@v0
        id: build
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          APP_VERSION: ${{ needs.get-version.outputs.version }}
          MSI_VERSION: ${{ needs.get-version.outputs.msi-version }}
          RELEASE_CHANNEL: ${{ needs.get-version.outputs.release-channel }}
        with:
          releaseId: ${{ inputs.release-id }}
          appVersion: ${{ matrix.platform == 'windows-latest' && env.MSI_VERSION && matrix.platform == 'windows-11-arm' && env.MSI_VERSION || env.APP_VERSION }}
          releaseDraft: true
          includeDebug: true
          includeRelease: true
          includeUpdaterJson: true
          args: ${{ matrix.args }}
      - name: Output Summary
        env:
          RELEASE_ID: ${{ steps.build.outputs.releaseId }}
          RELEASE_HTML_URL: ${{ steps.build.outputs.releaseHtmlUrl }}
          RELEASE_UPLOAD_URL: ${{ steps.build.outputs.releaseUploadUrl }}
          ARTIFACT_PATHS: "${{ steps.build.outputs.artifactPaths }}"
          APP_VERSION: ${{ steps.build.outputs.appVersion }}

        run: |
          printf "\n\n\n            ✈️      ✈️      \n\n"
          printf "\033[1mRelease Id : \033[32m${RELEASE_ID}\033[0m\n"
          printf "\033[1mRelease Html Url : \033[32m${RELEASE_HTML_URL}\033[0m\n"
          printf "\033[1mRelease Upload Url : \033[32m${RELEASE_UPLOAD_URL}\033[0m\n"
          printf "\033[1mArtifact Paths : \033[32m${ARTIFACT_PATHS}\033[0m\n"
          printf "\033[1mApp version : \033[32m${APP_VERSION}\033[0m\n"
          printf "\n      ✈️      ✈️      ✈️      \n\n\n\n "
